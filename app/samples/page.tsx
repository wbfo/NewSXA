import React from "react";
import { SamplesClient } from "@/components/samples-client";

export const metadata = {
  title: "Sample Intelligence Reports — Sovereign X Audits",
  description: "See what a Sovereign X audit actually looks like before you commit. Real findings. Real dollar figures. AICC Verified.",
};

export default function SamplesPage() {
  return <SamplesClient />;
}

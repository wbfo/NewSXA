export const operatingProtocols = `SOVEREIGN X AUDITS — OPERATING PROTOCOLS (SOPs)

P1 — AUDIT TRIGGERING
All Sovereign X Audits must be initiated with a verified Account Name and Category. Default to "Digital Standard" unless high-value growth potential is identified.

P2 — AGENT DISPATCH
Hermes is the primary orchestrator. Secondary agents (Research, Outreach) are dispatched only after Layer 1 verification is complete.

P3 — APPROVAL PIPELINE
Workflow steps requiring "Human-in-the-Loop" must be reviewed within 4 hours. Stale requests are automatically escalated to the Command level.

P4 — DATA INTEGRITY
All state changes must be logged in the Sovereign X Ledger. Concurrent modifications are managed via Mutex locks to prevent state corruption.`;
